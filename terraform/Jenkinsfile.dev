pipeline {
    agent any

    stages {
        stage('Simular CMD') {
            steps {
                script {
                    // Cambia temporalmente al directorio de Terraform
                    dir('C:\\Users\\anonimo\\Downloads\\terraform_1.7.3_windows_386') {
                        // Ahora deberías poder ejecutar Terraform
                        bat 'terraform --version'
                        // Otros comandos de Terraform
                    }
                }
            }
        }
    }
}
